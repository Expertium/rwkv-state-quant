# QAT decay — FULL-MATRIX int2 fake-quant (RWKV_QAT_SCOPE=card:int2,note:int2 in the cmd; element-wise, NO
# per-step SVD → much faster than low-rank QAT + the parent's proven-stable recipe). Warm-start the h2k16
# WS-final, cosine decay of EPOCHS=0.27 (~904 steps) — EXACTLY matching the champion h2k16d_904's decay so
# QAT-vs-no-QAT (champion) is a fair pair (QAT the only difference). Goal: improve the base + make int-quant ~free, then
# eval the low-rank/PQ deploy schemes as PTQ on the QAT'd weights (base improvement transfers — parent iter45).
TRAIN_USERS_START = 1000
TRAIN_USERS_END = 2499
VALIDATE_USERS_START = 101
VALIDATE_USERS_END = 110

TRAIN_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/train_db_sc8k_1500"
TRAIN_DATASET_LMDB_SIZE = 80_000_000_000
VALIDATE_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/test_db"
VALIDATE_DATASET_LMDB_SIZE = 8_000_000_000
LABEL_FILTER_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/label_filter_db"
LABEL_FILTER_LMDB_SIZE = 2_000_000_000

NUM_FETCH_PROCESSES = 6
MAX_TRAIN_GLOBAL_LEN = 66000

TRAIN_MODE = "D"
STEP_OFFSET = 1
WARMUP_STEPS = 0
EPOCHS = 0.27
VALIDATE_EVERY = 100000
PEAK_LR = 1e-3

LOAD_MODEL = true
LOAD_MODEL_FOLDER = "reference"
LOAD_MODEL_NAME = "h2k16ws_3351"
SAVE_MODEL_FOLDER = "reference"
SAVE_MODEL_PREFIX = "qat_fmi2"
DEVICE = "cuda"
DTYPE = "bfloat16"

USE_WANDB = false
WANDB_PROJECT_NAME = "rwkv"
WANDB_RESUME = false
WANDB_RESUME_ID = ""
