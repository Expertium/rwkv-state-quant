# PQ+QAT (F22): rank-1 low-rank WKV with the factor DIRECTIONS codebook-encoded (PQ m2b8) INSTEAD of int4,
# matching the deploy RWKV_LOWRANK_PQ scheme. Same recipe as the F15 winner (warm-start champion h2k16d_904,
# 0.1 ep, LR 1e-3), but RWKV_QAT_PQ=<codebook> makes the fused qat_lr_rank1 codebook-encode (train==deploy,
# parity-confirmed 3e-7). Goal: teach rank-1-PQ robustness so PTQ +0.0046 -> under the +0.0025 gate. card
# ~352 b (WKV PQ ~96 b + shift int4 256 b), sub-512 with margin.
TRAIN_USERS_START = 1000
TRAIN_USERS_END = 2499
VALIDATE_USERS_START = 101
VALIDATE_USERS_END = 110

TRAIN_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/train_db_sc8k_1500"
TRAIN_DATASET_LMDB_SIZE = 80_000_000_000
VALIDATE_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/test_db"
VALIDATE_DATASET_LMDB_SIZE = 8_000_000_000
LABEL_FILTER_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/label_filter_db"
LABEL_FILTER_LMDB_SIZE = 2_000_000_000

NUM_FETCH_PROCESSES = 6
MAX_TRAIN_GLOBAL_LEN = 66000

TRAIN_MODE = "D"
STEP_OFFSET = 1
WARMUP_STEPS = 0
EPOCHS = 0.1
VALIDATE_EVERY = 100000
PEAK_LR = 1e-3

LOAD_MODEL = true
LOAD_MODEL_FOLDER = "reference"
LOAD_MODEL_NAME = "h2k16d_904"
SAVE_MODEL_FOLDER = "reference"
SAVE_MODEL_PREFIX = "qat_pq_m2b8"
DEVICE = "cuda"
DTYPE = "bfloat16"

USE_WANDB = false
WANDB_PROJECT_NAME = "rwkv"
WANDB_RESUME = false
WANDB_RESUME_ID = ""
