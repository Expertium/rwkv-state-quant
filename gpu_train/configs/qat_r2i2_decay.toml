# QAT decay (rank-2 int2, the 256-b target): warm-start the h2k16 WS-final, cosine-decay with fake-quant ON
# (RWKV_QAT_LOWRANK_SCOPE=card:2:int2,note:2:int2 set in the launch cmd) so the net learns weights robust to the
# deploy compression. Warm-start REQUIRES reference/h2k16ws_3351.pth + reference/h2k16ws_3351_optim.pth.
# LMDBs are REFERENCED from the parent repo (read-only training data). ~0.5 epoch (~1.6k steps, ~12 min GPU).
TRAIN_USERS_START = 1000
TRAIN_USERS_END = 2499
VALIDATE_USERS_START = 101
VALIDATE_USERS_END = 110

TRAIN_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/train_db_sc8k_1500"
TRAIN_DATASET_LMDB_SIZE = 80_000_000_000
VALIDATE_DATASET_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/test_db"
VALIDATE_DATASET_LMDB_SIZE = 8_000_000_000
LABEL_FILTER_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/label_filter_db"
LABEL_FILTER_LMDB_SIZE = 2_000_000_000

NUM_FETCH_PROCESSES = 6
MAX_TRAIN_GLOBAL_LEN = 66000

TRAIN_MODE = "D"
STEP_OFFSET = 1
WARMUP_STEPS = 0
EPOCHS = 0.5
VALIDATE_EVERY = 100000
PEAK_LR = 1e-3

LOAD_MODEL = true
LOAD_MODEL_FOLDER = "reference"
LOAD_MODEL_NAME = "h2k16ws_3351"
SAVE_MODEL_FOLDER = "reference"
SAVE_MODEL_PREFIX = "qat_r2i2"
DEVICE = "cuda"
DTYPE = "bfloat16"

USE_WANDB = false
WANDB_PROJECT_NAME = "rwkv"
WANDB_RESUME = false
WANDB_RESUME_ID = ""
