# GPU eval: q56k (learned m1b5 joint WKV + learned m4b4 shifts + learned ROTATION + 1-bit norms = 64 b).
# The rotation loads from the exported file (RWKV_QAT_SHIFT_ROT=<path> in the run cmd).
FILE_AHEAD = "gpuval-q56k"
FILE_IMM = "gpuval-q56k-P"

MODEL_PATH = "reference/qat_pq_q56k_6702.pth"
DEVICE = "cuda"
DTYPE = "bfloat16"

DATASET_LMDB_PATH = "F:/rwkv_lmdb/test_db_5k"
DATASET_LMDB_SIZE = 250_000_000_000
LABEL_FILTER_LMDB_PATH = "C:/Users/Andrew/rwkv-anki-autoresearch/label_filter_db"
LABEL_FILTER_LMDB_SIZE = 40_000_000_000

RAW = false
RAW_DB_PATH = "raw/result_db"
RAW_DB_SIZE = 3_000_000_000

USER_START = 0
USER_END = 0
USERS_FILE = "val_users.json"

NUM_FETCH_PROCESSES = 5
