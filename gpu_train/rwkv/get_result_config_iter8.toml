# Iteration 8 eval: d=32 arch (same as iter7) but WS-ONLY checkpoint (no decay phase).
# Tests whether skipping decay recovers imm for d=32. Model = the iter7 WS-558 checkpoint.
FILE_AHEAD = "RWKV-iter8"
FILE_IMM = "RWKV-P-iter8"

MODEL_PATH = "pretrain/rwkv/opt_iter7/rwkv_iter7_558.pth"
DEVICE = "cuda"
DTYPE = "bfloat16"

DATASET_LMDB_PATH = "test_db"
DATASET_LMDB_SIZE = 8_000_000_000
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 1_000_000_000

RAW = false
RAW_DB_PATH = "raw/result_db"
RAW_DB_SIZE = 1_000_000_000

USER_START = 101
USER_END = 200

NUM_FETCH_PROCESSES = 7
