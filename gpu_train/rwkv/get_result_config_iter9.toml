# Iteration 9 eval: d=32 (N_HEADS 1), layers [3,4,2,3,4], LoRAs 8, WS-ONLY (no decay).
# Model = the iter9 WS-558 checkpoint.
FILE_AHEAD = "RWKV-iter9"
FILE_IMM = "RWKV-P-iter9"

MODEL_PATH = "pretrain/rwkv/opt_iter9/rwkv_iter9_558.pth"
DEVICE = "cuda"
DTYPE = "bfloat16"

DATASET_LMDB_PATH = "test_db"
DATASET_LMDB_SIZE = 8_000_000_000
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 1_000_000_000

RAW = false
RAW_DB_PATH = "raw/result_db"
RAW_DB_SIZE = 1_000_000_000

USER_START = 101
USER_END = 200

NUM_FETCH_PROCESSES = 7
