# Route-R (a2): even-smaller-chunk train_db for users 1-100. MAX_BATCH_SIZE=8192 -> 4096-review chunks
# (allowable_size = MAX_BATCH_SIZE//2). Tests whether the smaller-chunk accuracy win scales further.
# Separate LMDB (train_db_sc4k). label_filter_db already covers 1-100.
DEVICE = "cuda"
DTYPE = "bfloat16"
DATA_PATH = "../anki-revlogs-10k"

LMDB_PATH = "train_db_sc4k"
LMDB_SIZE = 4_000_000_000
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 10_000_000_000

USER_START = 1
USER_END = 100
MAX_BATCH_SIZE = 8192
RWKV_SUBMODULES = ["card_id", "note_id", "deck_id", "preset_id", "user_id"]
