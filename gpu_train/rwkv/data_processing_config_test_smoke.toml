# SMOKE: build test_db for users 1-5 only (they already have label_filter_db).
# test_db keeps users whole (large MAX_BATCH_SIZE) for evaluation by get_result.py.
DEVICE = "cuda"
DTYPE = "bfloat16"
DATA_PATH = "../anki-revlogs-10k"

LMDB_PATH = "test_db"
LMDB_SIZE = 2_000_000_000          # 2 GB reserved (users 1-5 are tiny); Windows pre-allocates
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 500_000_000   # matches the 1-5 db already built

USER_START = 1
USER_END = 5
MAX_BATCH_SIZE = 2097152
PROCESSES = 5                       # <= 7-thread cap
RWKV_SUBMODULES = ["card_id", "note_id", "deck_id", "preset_id", "user_id"]
